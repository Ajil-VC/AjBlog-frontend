import { RightclickpreventorDirective } from './rightclickpreventor.directive';

describe('RightclickpreventorDirective', () => {
  it('should create an instance', () => {
    const directive = new RightclickpreventorDirective();
    expect(directive).toBeTruthy();
  });
});
